# Dependencies required for emacs' elpy package, providing improved editor
# support for Python development.

# Let pip and setuptools float... They might be stable enough that it is safe
# to do so.  Also, mach-nix does things with pip and setuptools and I'm afraid
# that pinning versions here will conflict with it (though I have no direct
# evidence of this).
pip
setuptools

# This does the heavy lifting related to Python.
jedi == 0.18.1
